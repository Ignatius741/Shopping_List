Hi, This is the Instructions on how to Setup and Run this system:

Install the dependencies which is called node_modules 
CMD/Terminal
"npm install"

Start the server through CMD/Terminal
"npm start"

After Running your server go to http://localhost:3000

Don't forget to do Ctrl+C in the terminal if you want to shutdown the server and update the db.json.
